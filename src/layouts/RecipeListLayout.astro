---
import Navigation from "../components/Navigation.astro";
import Title from "../components/Title.astro";
import { SITE_DATA } from "../data";
import BaseLayout from "../layouts/BaseLayout.astro";

export interface Props {
    title: string;
}

const { title } = Astro.props as Props;
---

<BaseLayout title={title}>
    <Navigation />

    <Title />

    <section class="px-2 md:my-8">
        <div class="prose-custom mx-auto max-w-[56ch]">
            <p>
                If you're reading this, you are either inspecting the source
                code or have found my little secret! 😉
            </p>
            <p>
                I wanted an easily accessible place to keep my cookbook, and
                this place serves as well as any other. Sadly, the recipes are
                only available in Dutch, but let me know if you try them at{" "}
                <a href={`mailto:${SITE_DATA.email}`}>{SITE_DATA.email}</a>.
            </p>
        </div>

        <slot />
    </section>
</BaseLayout>
