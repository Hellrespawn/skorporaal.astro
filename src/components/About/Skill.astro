---
export interface Props {
  src: string;
  name: string;
  class?: string;
  dark?: boolean;
  png?: boolean;
}

const {
  src,
  name,
  class: classes = '',
  dark = false,
  png = false,
} = Astro.props as Props;

const extension = png ? 'png' : 'svg';
---

<div class="my-4 flex flex-row items-center">
  <div
    class={`w-20 h-20 flex-shrink-0 bg-gray-100 border border-gray-200 rounded-r-full overflow-hidden dark:bg-gray-700 dark:border-gray-900 dark:border-none ${classes}`.trim()}
  >
    <img
      src={`/assets/skills/${src}${dark ? '-light' : ''}.${extension}`}
      class={`${dark ? ' dark:hidden' : ''} w-full h-full`}
      alt={`${name} logo`}
    />
    {
      dark && (
        <img
          src={`/assets/skills/${src}-dark.${extension}`}
          class="hidden h-full w-full dark:block"
          alt={`${name} logo`}
        />
      )
    }
  </div>
  <div class="flex-grow pl-4 pr-2">
    <h3 class="text-lg font-semibold">{name}</h3>
    <slot />
  </div>
</div>
