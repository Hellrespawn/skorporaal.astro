---
import Feed from './FeedComponent.vue';
import Filter from './FilterComponent.vue';
import type { EntryWithCategory } from '../../collection';

export interface Props {
  entriesWithCategory: EntryWithCategory;
}

const { entriesWithCategory } = Astro.props as Props;
---

<Filter client:only="vue" />
<Feed entriesWithCategory={entriesWithCategory} client:only="vue" />
