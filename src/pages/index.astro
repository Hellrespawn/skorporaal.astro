---
import PostLayout from "@layouts/PostLayout.astro";
import { Debug } from 'astro/components';
import { Post } from "@scripts/post";

const posts = (await Astro.glob<Post>('../content/**/*.md'));

const post = posts[8];

---

<PostLayout>
    <p>title: { post.frontmatter.title }</p>

    <post.Content />

</PostLayout>
