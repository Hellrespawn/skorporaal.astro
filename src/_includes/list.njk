<div
  x-data="{
    filter: '',

    toggle(tag) {
      this.filter === tag ? this.filter = '' : this.filter = tag
    }
  }"
>
  {% include 'filter.njk' %}

  <ul>
    {% for item in collections.all | filterPosts %}
      {% include "list-heading.njk" %}
    {% endfor %}
  </ul>
</div>
