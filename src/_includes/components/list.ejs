<% let items = collections.all.filter(item => typeof item.data.tags !==
'undefined').reverse(); %>

<ul>
  <% for (const item of items) { %> <%- include("/partials/list-heading", { item
  }) %> <% } %>
</ul>
